{
  "name": "phaser-multiplayer-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "typescript": "^5.0.2",
    "vite": "5.1.4"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "firebase": "^10.5.2",
    "phaser": "^3.60.0"
  }
}
